int fib(int n) {
    int a = 0;
    int b = 1;

    // if (n <= 1) return a;
    // else if (n == 2) return b;
    // int next;
    // loop (n - 2) {
    //     next = a + b;
    //     a = b;
    //     b = next;
    // }
    // return next;
}